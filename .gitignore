# The sandbox workspace (sbxenv.yaml: workspace: ./dev). It belongs to the
# sandbox: never run git, scripts or build tools in it from the host, and never
# `git clean -x` here (it would delete the workspace).
/dev/
